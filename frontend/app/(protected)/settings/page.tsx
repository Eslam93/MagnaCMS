/**
 * Stub for /settings. Real settings page lands later (no specific
 * phase pinned yet — likely P10 with the polish pass).
 */
export default function SettingsStub() {
  return (
    <div className="rounded-lg border bg-card p-8 text-center">
      <h1 className="text-xl font-bold">Settings</h1>
      <p className="mt-2 text-sm text-muted-foreground">Settings UI lands later in the build.</p>
    </div>
  );
}
