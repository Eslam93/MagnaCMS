# Lockfile is owned by pnpm — its formatting is dictated by the tool,
# not the repo's prettier config.
pnpm-lock.yaml

# Build outputs
.next
node_modules
out

# Generated API types (regenerated by `pnpm gen:api`).
types/api.d.ts
